spring:
  application:
    name: SPRINGBOOTRESTENDTOENDAPPLICATION
  datasource:
    #driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${MYSQL_USER:root}
    url: jdbc:mysql://database-1.cro0q6igmpws.ap-south-2.rds.amazonaws.com:3306}/techiendtoendapp?useSSL=false&allowPublicKeyRetrieval=true
    password: ${MYSQL_PASSWORD:mysqlroot}
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
#  profiles:
#    active:
#    - test
logging:
  file:
    name: C:/Users/Home/Desktop/logs/springbootapplication.log
management:
  tracing:
    sampling:
      probability: 1.0
server:
  port: 2026