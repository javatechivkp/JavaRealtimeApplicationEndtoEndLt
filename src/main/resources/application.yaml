spring:
  application:
    name: SPRINGBOOTRESTENDTOENDAPPLICATION
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/techiendtoendapp?allowPublicKeyRetrieval=true&useSSL=false
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
#  profiles:
#    active:
#    - test
logging:
  file:
    name: C:/Users/Srinu/Desktop/logs/springbootapplication.log
management:
  tracing:
    sampling:
      probability: 1.0
server:
  port: 2026